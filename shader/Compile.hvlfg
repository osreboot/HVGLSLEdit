uniform sampler2D texture1;
void main(){
vec4 color = texture2D(texture1, gl_TexCoord[0].st);
vec4 test2 = texture2D(texture1, vec2(gl_TexCoord[0].x,gl_TexCoord[0].y));
for(float f = 0; f < 30.0; f++){
test2 = (vec4(1.0,0.0,0.0,0.047058824) + test2);
}
gl_FragColor = test2;
}
